spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/felizvitoz/config-server.git
          basedir: application-config

server:
  port: 8888