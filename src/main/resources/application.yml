spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/felizvitoz/config-server
          #basedir: application-config

server:
  port: 8889